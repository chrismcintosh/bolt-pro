jQuery(document).ready(($) => {
     // Add slick.js to anywhere you put a class of slick
     $('.slick').slick({
          dots: true,
          adaptiveHeight: true,
          infinite: true,
          speed: 500,
          fade: true,
          cssEase: 'linear',
     }).foundation();
});
