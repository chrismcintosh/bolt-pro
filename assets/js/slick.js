jQuery(document).ready(function ($) {
     // Add slick.js to anywhere you put a class of slick
     jQuery('.slick').slick({
          dots: true,
          adaptiveHeight: true,
          infinite: true,
          speed: 500,
          fade: true,
          cssEase: 'linear'
     }).foundation();
});
